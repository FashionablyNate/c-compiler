# C Compiler

A C compiler written in ARM assembly, made to run on the apple m1 pro chip.

## How to build

1. Clone the repository
2. Run `make`
3. The executable should be generated in the `bin` directory and can be called by typing `bin/main`.